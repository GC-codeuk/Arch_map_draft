
  <div id="infobox">
  	  <div id="infobox-img">
        <img src="<%= plot.img_link %>">
      </div>
      <div id="infobox-text">
        <%= hidden_field_tag('plot_id', plot.id) %>
        <h3><%= plot.title %></h3>
        <p>Architect: <%= plot.architect.name %></p>
        <p><%= plot.address %></p>
        <p>More details ></p>
      </div>
  </div>

 